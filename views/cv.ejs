<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="icon" type="image/png" href="https://cdn-icons-png.flaticon.com/512/6361/6361475.png">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        body {
            font-family: 'Inter', sans-serif;
            background: #f8fafc;
            margin: 0;
            padding: 0;
            overflow-x: hidden;
        }

        .cv-wrapper {
            min-height: 100vh;
            padding: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .cv-actions {
            position: fixed;
            top: 20px;
            right: 20px;
            display: flex;
            flex-direction: column;
            gap: 10px;
            z-index: 1000;
        }

        .cv-action-btn {
            background: #3b82f6;
            color: white;
            border: none;
            padding: 12px 20px;
            border-radius: 8px;
            cursor: pointer;
            font-weight: 500;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(59, 130, 246, 0.3);
            display: flex;
            align-items: center;
            gap: 8px;
            font-size: 0.9rem;
        }

        .cv-action-btn:hover {
            background: #1d4ed8;
            transform: translateY(-2px);
        }

        .cv-action-btn.secondary {
            background: #64748b;
        }

        .cv-action-btn.secondary:hover {
            background: #475569;
        }

        .pdf-container {
            width: 100%;
            max-width: 1200px;
            height: calc(100vh - 40px);
            background: white;
            border-radius: 10px;
            box-shadow: 0 10px 40px rgba(0,0,0,0.1);
            overflow: hidden;
            display: flex;
            flex-direction: column;
        }

        .pdf-viewer {
            flex: 1;
            width: 100%;
            border: none;
            background: #e5e7eb;
        }

        .pdf-error {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100%;
            padding: 40px;
            text-align: center;
            color: #64748b;
        }

        .pdf-error i {
            font-size: 4rem;
            margin-bottom: 20px;
            color: #cbd5e1;
        }

        .pdf-error h2 {
            font-size: 1.5rem;
            margin-bottom: 10px;
            color: #1e293b;
        }

        .pdf-error p {
            font-size: 1rem;
            margin-bottom: 20px;
            max-width: 500px;
        }

        .loading-spinner {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100%;
            color: #64748b;
        }

        .spinner {
            border: 4px solid #e5e7eb;
            border-top: 4px solid #3b82f6;
            border-radius: 50%;
            width: 50px;
            height: 50px;
            animation: spin 1s linear infinite;
            margin-bottom: 20px;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        @media (max-width: 768px) {
            .cv-wrapper {
                padding: 10px;
            }

            .cv-actions {
                position: static;
                margin: 10px 0 20px 0;
                flex-direction: row;
                justify-content: center;
                flex-wrap: wrap;
                width: 100%;
            }

            .pdf-container {
                height: calc(100vh - 120px);
                border-radius: 8px;
            }

            .cv-action-btn {
                padding: 10px 16px;
                font-size: 0.85rem;
            }
        }

        @media print {
            .cv-actions {
                display: none;
            }

            .cv-wrapper {
                padding: 0;
            }

            .pdf-container {
                box-shadow: none;
                border-radius: 0;
                height: 100vh;
            }
        }
    </style>
</head>
<body>
    <div class="cv-actions">
        <button class="cv-action-btn secondary" onclick="downloadPDF()">
            <i class="fas fa-download"></i> Download PDF
        </button>
        <button class="cv-action-btn" onclick="printPDF()">
            <i class="fas fa-print"></i> Print
        </button>
        <button class="cv-action-btn secondary" onclick="goBack()">
            <i class="fas fa-arrow-left"></i> Back
        </button>
    </div>

    <div class="cv-wrapper">
        <div class="pdf-container">
            <iframe 
                id="pdfViewer" 
                class="pdf-viewer" 
                src="/pdfs/cv.pdf" 
                type="application/pdf"
                onload="handlePDFLoad()"
                onerror="handlePDFError()">
            </iframe>
            <div id="loadingSpinner" class="loading-spinner">
                <div class="spinner"></div>
                <p>Loading CV...</p>
            </div>
            <div id="pdfError" class="pdf-error" style="display: none;">
                <i class="fas fa-file-pdf"></i>
                <h2>CV Not Found</h2>
                <p>Please make sure your CV PDF file is placed in the <code>public/pdfs/</code> directory and named <code>cv.pdf</code></p>
                <p style="font-size: 0.9rem; color: #94a3b8; margin-top: 10px;">
                    <strong>Instructions:</strong><br>
                    1. Place your CV PDF file in: <code>public/pdfs/cv.pdf</code><br>
                    2. Refresh this page to view your CV
                </p>
            </div>
        </div>
    </div>

    <script>
        const pdfViewer = document.getElementById('pdfViewer');
        const loadingSpinner = document.getElementById('loadingSpinner');
        const pdfError = document.getElementById('pdfError');
        let loadTimeout;

        function handlePDFLoad() {
            clearTimeout(loadTimeout);
            // Check if PDF actually loaded by waiting a bit
            loadTimeout = setTimeout(() => {
                try {
                    // Try to access the iframe content to check if PDF loaded
                    const iframeDoc = pdfViewer.contentDocument || pdfViewer.contentWindow.document;
                    if (iframeDoc && iframeDoc.body) {
                        // PDF might have loaded, hide spinner
                        loadingSpinner.style.display = 'none';
                        pdfError.style.display = 'none';
                    }
                } catch (e) {
                    // Cross-origin or other issue, assume PDF is loading/loaded
                    // Hide spinner after a delay
                    setTimeout(() => {
                        loadingSpinner.style.display = 'none';
                    }, 2000);
                }
            }, 1000);
        }

        function handlePDFError() {
            clearTimeout(loadTimeout);
            loadingSpinner.style.display = 'none';
            pdfViewer.style.display = 'none';
            pdfError.style.display = 'flex';
        }

        // Check if PDF loads successfully
        window.addEventListener('load', function() {
            // Set a timeout to show error if PDF doesn't load
            setTimeout(() => {
                // Check if iframe still shows loading
                try {
                    const iframe = pdfViewer;
                    if (iframe.contentWindow.location.href === 'about:blank' || 
                        iframe.contentDocument === null) {
                        // PDF might not be loading, check after another delay
                        setTimeout(() => {
                            if (loadingSpinner.style.display !== 'none') {
                                // Still loading, might be an issue
                                // Try alternative method
                                fetch('/pdfs/cv.pdf')
                                    .then(response => {
                                        if (!response.ok) {
                                            handlePDFError();
                                        } else {
                                            loadingSpinner.style.display = 'none';
                                        }
                                    })
                                    .catch(() => {
                                        handlePDFError();
                                    });
                            }
                        }, 2000);
                    }
                } catch (e) {
                    // Cross-origin, assume it's loading normally
                    setTimeout(() => {
                        loadingSpinner.style.display = 'none';
                    }, 2000);
                }
            }, 3000);
        });

        function downloadPDF() {
            const link = document.createElement('a');
            link.href = '/pdfs/cv.pdf';
            link.download = 'cv.pdf';
            link.target = '_blank';
            document.body.appendChild(link);
            link.click();
            document.body.removeChild(link);
        }

        function printPDF() {
            const iframe = document.getElementById('pdfViewer');
            try {
                iframe.contentWindow.print();
            } catch (e) {
                // Fallback: open PDF in new window for printing
                window.open('/pdfs/cv.pdf', '_blank');
            }
        }

        function goBack() {
            window.history.back();
        }
    </script>
</body>
</html>
