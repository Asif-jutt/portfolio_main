<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="icon" type="image/png" href="https://cdn-icons-png.flaticon.com/512/6361/6361475.png">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        body {
            font-family: 'Inter', sans-serif;
            line-height: 1.6;
            color: #333;
            background: #f8fafc;
            margin: 0;
            padding: 20px;
        }

        .cv-container {
            max-width: 210mm;
            margin: 0 auto;
            background: white;
            box-shadow: 0 0 20px rgba(0,0,0,0.1);
            border-radius: 10px;
            overflow: hidden;
        }

        .cv-header {
            background: linear-gradient(135deg, #1e293b 0%, #334155 100%);
            color: white;
            padding: 40px;
            text-align: center;
            position: relative;
        }

        .cv-header::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, #3b82f6, #1d4ed8, #059669);
        }

        .profile-image {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            border: 4px solid white;
            margin: 0 auto 20px;
            background: url('/images/profilePic.jpeg') center/cover;
            box-shadow: 0 4px 15px rgba(0,0,0,0.2);
        }

        .cv-name {
            font-size: 2.5rem;
            font-weight: 700;
            margin: 0 0 10px 0;
            color: white;
        }

        .cv-title {
            font-size: 1.2rem;
            font-weight: 400;
            margin: 0 0 20px 0;
            color: #cbd5e1;
        }

        .contact-info {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            gap: 20px;
            margin-top: 20px;
        }

        .contact-item {
            display: flex;
            align-items: center;
            gap: 8px;
            color: #e2e8f0;
            font-size: 0.9rem;
        }

        .contact-item i {
            width: 16px;
            text-align: center;
            color: #3b82f6;
        }

        .contact-item a {
            color: #e2e8f0;
            text-decoration: none;
            transition: color 0.3s ease;
        }

        .contact-item a:hover {
            color: #3b82f6;
        }

        .cv-content {
            padding: 40px;
        }

        .section {
            margin-bottom: 30px;
        }

        .section-title {
            font-size: 1.4rem;
            font-weight: 600;
            color: #1e293b;
            margin-bottom: 15px;
            padding-bottom: 8px;
            border-bottom: 2px solid #e2e8f0;
            position: relative;
        }

        .section-title::after {
            content: '';
            position: absolute;
            bottom: -2px;
            left: 0;
            width: 50px;
            height: 2px;
            background: linear-gradient(90deg, #3b82f6, #1d4ed8);
        }

        .summary-text {
            font-size: 1rem;
            line-height: 1.7;
            color: #475569;
            text-align: justify;
        }

        .education-item {
            background: #f8fafc;
            padding: 20px;
            border-radius: 8px;
            border-left: 4px solid #3b82f6;
            margin-bottom: 15px;
        }

        .education-degree {
            font-size: 1.1rem;
            font-weight: 600;
            color: #1e293b;
            margin-bottom: 5px;
        }

        .education-institution {
            font-size: 1rem;
            color: #3b82f6;
            font-weight: 500;
            margin-bottom: 8px;
        }

        .education-period {
            font-size: 0.9rem;
            color: #64748b;
            margin-bottom: 10px;
        }

        .education-achievements {
            display: flex;
            gap: 10px;
            flex-wrap: wrap;
            margin-bottom: 8px;
        }

        .achievement-badge {
            background: #059669;
            color: white;
            padding: 4px 8px;
            border-radius: 12px;
            font-size: 0.8rem;
            font-weight: 500;
        }

        .education-gpa {
            font-size: 0.9rem;
            color: #059669;
            font-weight: 600;
        }

        .skills-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
        }

        .skill-category {
            background: #f8fafc;
            padding: 15px;
            border-radius: 8px;
            border: 1px solid #e2e8f0;
        }

        .skill-category-name {
            font-weight: 600;
            color: #1e293b;
            margin-bottom: 10px;
            font-size: 1rem;
        }

        .skill-tags {
            display: flex;
            flex-wrap: wrap;
            gap: 6px;
        }

        .skill-tag {
            background: #e0e7ff;
            color: #3730a3;
            padding: 4px 8px;
            border-radius: 6px;
            font-size: 0.8rem;
            font-weight: 500;
        }

        .project-item {
            background: #f8fafc;
            padding: 20px;
            border-radius: 8px;
            border-left: 4px solid #059669;
            margin-bottom: 15px;
        }

        .project-name {
            font-size: 1.1rem;
            font-weight: 600;
            color: #1e293b;
            margin-bottom: 8px;
        }

        .project-description {
            color: #475569;
            margin-bottom: 10px;
            line-height: 1.6;
        }

        .project-technologies {
            display: flex;
            flex-wrap: wrap;
            gap: 6px;
            margin-bottom: 10px;
        }

        .tech-tag {
            background: #fef3c7;
            color: #d97706;
            padding: 3px 8px;
            border-radius: 6px;
            font-size: 0.8rem;
            font-weight: 500;
        }

        .project-links {
            display: flex;
            gap: 15px;
        }

        .project-link {
            display: flex;
            align-items: center;
            gap: 5px;
            color: #3b82f6;
            text-decoration: none;
            font-size: 0.9rem;
            font-weight: 500;
            transition: color 0.3s ease;
        }

        .project-link:hover {
            color: #1d4ed8;
        }

        .certification-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 12px 0;
            border-bottom: 1px solid #e2e8f0;
        }

        .certification-item:last-child {
            border-bottom: none;
        }

        .certification-info {
            flex: 1;
        }

        .certification-name {
            font-weight: 600;
            color: #1e293b;
            margin-bottom: 3px;
        }

        .certification-issuer {
            color: #64748b;
            font-size: 0.9rem;
        }

        .certification-date {
            color: #64748b;
            font-size: 0.9rem;
            margin-right: 10px;
        }

        .certification-status {
            padding: 4px 8px;
            border-radius: 12px;
            font-size: 0.8rem;
            font-weight: 500;
        }

        .status-completed {
            background: #d1fae5;
            color: #059669;
        }

        .status-in-progress {
            background: #fef3c7;
            color: #d97706;
        }

        .achievements-list {
            list-style: none;
            padding: 0;
        }

        .achievements-list li {
            padding: 8px 0;
            padding-left: 20px;
            position: relative;
            color: #475569;
        }

        .achievements-list li::before {
            content: '✓';
            position: absolute;
            left: 0;
            color: #059669;
            font-weight: bold;
        }

        .languages-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 15px;
        }

        .language-item {
            background: #f8fafc;
            padding: 15px;
            border-radius: 8px;
            text-align: center;
            border: 1px solid #e2e8f0;
        }

        .language-name {
            font-weight: 600;
            color: #1e293b;
            margin-bottom: 5px;
        }

        .language-proficiency {
            color: #64748b;
            font-size: 0.9rem;
        }

        .cv-actions {
            position: fixed;
            top: 20px;
            right: 20px;
            display: flex;
            flex-direction: column;
            gap: 10px;
            z-index: 1000;
        }

        .cv-action-btn {
            background: #3b82f6;
            color: white;
            border: none;
            padding: 12px 20px;
            border-radius: 8px;
            cursor: pointer;
            font-weight: 500;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(59, 130, 246, 0.3);
        }

        .cv-action-btn:hover {
            background: #1d4ed8;
            transform: translateY(-2px);
        }

        .cv-action-btn.secondary {
            background: #64748b;
        }

        .cv-action-btn.secondary:hover {
            background: #475569;
        }

        @media print {
            body {
                background: white;
                padding: 0;
            }
            
            .cv-container {
                box-shadow: none;
                border-radius: 0;
                max-width: none;
            }
            
            .cv-actions {
                display: none;
            }
            
            .cv-content {
                padding: 30px;
            }
            
            .cv-header {
                padding: 30px;
            }
        }

        @media (max-width: 768px) {
            .cv-header {
                padding: 30px 20px;
            }
            
            .cv-content {
                padding: 30px 20px;
            }
            
            .cv-name {
                font-size: 2rem;
            }
            
            .contact-info {
                flex-direction: column;
                gap: 10px;
            }
            
            .skills-grid {
                grid-template-columns: 1fr;
            }
            
            .project-links {
                flex-direction: column;
                gap: 10px;
            }
            
            .cv-actions {
                position: static;
                margin: 20px 0;
                flex-direction: row;
                justify-content: center;
            }
        }
    </style>
</head>
<body>
    <div class="cv-actions">
        <button class="cv-action-btn" onclick="printCV()">
            <i class="fas fa-print"></i> Print CV
        </button>
        <button class="cv-action-btn secondary" onclick="downloadPDF()">
            <i class="fas fa-download"></i> Download PDF
        </button>
        <button class="cv-action-btn secondary" onclick="goBack()">
            <i class="fas fa-arrow-left"></i> Back to Portfolio
        </button>
    </div>

    <div class="cv-container">
        <!-- CV Header -->
        <div class="cv-header">
            <div class="profile-image"></div>
            <h1 class="cv-name"><%= cvData.personalInfo.name %></h1>
            <p class="cv-title"><%= cvData.personalInfo.title %></p>
            
            <div class="contact-info">
                <div class="contact-item">
                    <i class="fas fa-envelope"></i>
                    <a href="mailto:<%= cvData.personalInfo.email %>"><%= cvData.personalInfo.email %></a>
                </div>
                <div class="contact-item">
                    <i class="fas fa-phone"></i>
                    <span><%= cvData.personalInfo.phone %></span>
                </div>
                <div class="contact-item">
                    <i class="fas fa-map-marker-alt"></i>
                    <span><%= cvData.personalInfo.location %></span>
                </div>
                <div class="contact-item">
                    <i class="fab fa-github"></i>
                    <a href="<%= cvData.personalInfo.github %>" target="_blank">GitHub Profile</a>
                </div>
                <div class="contact-item">
                    <i class="fas fa-globe"></i>
                    <a href="<%= cvData.personalInfo.website %>" target="_blank">Portfolio Website</a>
                </div>
            </div>
        </div>

        <!-- CV Content -->
        <div class="cv-content">
            <!-- Professional Summary -->
            <div class="section">
                <h2 class="section-title"><%= cvData.summary.title %></h2>
                <p class="summary-text"><%= cvData.summary.content %></p>
            </div>

            <!-- Education -->
            <div class="section">
                <h2 class="section-title"><%= cvData.education.title %></h2>
                <% cvData.education.items.forEach(edu => { %>
                    <div class="education-item">
                        <div class="education-degree"><%= edu.degree %></div>
                        <div class="education-institution"><%= edu.institution %></div>
                        <div class="education-period"><%= edu.period %></div>
                        <div class="education-achievements">
                            <% edu.achievements.forEach(achievement => { %>
                                <span class="achievement-badge"><%= achievement %></span>
                            <% }); %>
                        </div>
                        <div class="education-gpa">GPA: <%= edu.gpa %></div>
                    </div>
                <% }); %>
            </div>

            <!-- Technical Skills -->
            <div class="section">
                <h2 class="section-title"><%= cvData.skills.title %></h2>
                <div class="skills-grid">
                    <% cvData.skills.categories.forEach(category => { %>
                        <div class="skill-category">
                            <div class="skill-category-name"><%= category.name %></div>
                            <div class="skill-tags">
                                <% category.skills.forEach(skill => { %>
                                    <span class="skill-tag"><%= skill %></span>
                                <% }); %>
                            </div>
                        </div>
                    <% }); %>
                </div>
            </div>

            <!-- Projects -->
            <div class="section">
                <h2 class="section-title"><%= cvData.projects.title %></h2>
                <% cvData.projects.items.forEach(project => { %>
                    <div class="project-item">
                        <div class="project-name"><%= project.name %></div>
                        <div class="project-description"><%= project.description %></div>
                        <div class="project-technologies">
                            <% project.technologies.forEach(tech => { %>
                                <span class="tech-tag"><%= tech %></span>
                            <% }); %>
                        </div>
                        <div class="project-links">
                            <a href="<%= project.github %>" target="_blank" class="project-link">
                                <i class="fab fa-github"></i> GitHub
                            </a>
                            <a href="<%= project.demo %>" target="_blank" class="project-link">
                                <i class="fas fa-external-link-alt"></i> Live Demo
                            </a>
                        </div>
                    </div>
                <% }); %>
            </div>

            <!-- Certifications -->
            <div class="section">
                <h2 class="section-title"><%= cvData.certifications.title %></h2>
                <% cvData.certifications.items.forEach(cert => { %>
                    <div class="certification-item">
                        <div class="certification-info">
                            <div class="certification-name"><%= cert.name %></div>
                            <div class="certification-issuer"><%= cert.issuer %></div>
                        </div>
                        <div style="display: flex; align-items: center; gap: 10px;">
                            <span class="certification-date"><%= cert.date %></span>
                            <span class="certification-status <%= cert.status.toLowerCase().replace(' ', '-') %>">
                                <%= cert.status %>
                            </span>
                        </div>
                    </div>
                <% }); %>
            </div>

            <!-- Achievements -->
            <div class="section">
                <h2 class="section-title"><%= cvData.achievements.title %></h2>
                <ul class="achievements-list">
                    <% cvData.achievements.items.forEach(achievement => { %>
                        <li><%= achievement %></li>
                    <% }); %>
                </ul>
            </div>

            <!-- Languages -->
            <div class="section">
                <h2 class="section-title"><%= cvData.languages.title %></h2>
                <div class="languages-grid">
                    <% cvData.languages.items.forEach(lang => { %>
                        <div class="language-item">
                            <div class="language-name"><%= lang.language %></div>
                            <div class="language-proficiency"><%= lang.proficiency %></div>
                        </div>
                    <% }); %>
                </div>
            </div>
        </div>
    </div>

    <script>
        function printCV() {
            window.print();
        }

        function downloadPDF() {
            // For now, we'll use the browser's print to PDF functionality
            // In a production environment, you could integrate with a PDF generation library
            alert('Please use the Print function and select "Save as PDF" to download your CV.');
            window.print();
        }

        function goBack() {
            window.history.back();
        }

        // Add some interactivity
        document.addEventListener('DOMContentLoaded', function() {
            // Add hover effects to project items
            const projectItems = document.querySelectorAll('.project-item');
            projectItems.forEach(item => {
                item.addEventListener('mouseenter', function() {
                    this.style.transform = 'translateY(-2px)';
                    this.style.boxShadow = '0 8px 25px rgba(0,0,0,0.1)';
                });
                
                item.addEventListener('mouseleave', function() {
                    this.style.transform = 'translateY(0)';
                    this.style.boxShadow = 'none';
                });
            });

            // Add smooth transitions
            const sections = document.querySelectorAll('.section');
            sections.forEach(section => {
                section.style.transition = 'all 0.3s ease';
            });
        });
    </script>
</body>
</html>
